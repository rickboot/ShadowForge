// Maximum input size accepted by the API (characters, not tokens)
// ~20K chars ≈ ~5K tokens — enough for a full dungeon level
export const MAX_INPUT_CHARS = 20_000;

// LLM request timeout in milliseconds
export const LLM_TIMEOUT_MS = 60_000;
